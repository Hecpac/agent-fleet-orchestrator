# Smoke en vivo: R1 (submit declarativo) + F0 (opinion)

Encarnación verificada: `main@a754039`, working tree limpio. cmux arrancado
para el smoke (socket rechazado al inicio; `open -a cmux` → PONG).

## Veredicto R1 — Codex: **PASS**

Fleet real `r1smoke-codex` (preset small, lead codex `gpt-5.6-sol`):

- El manifest booteado carga la semántica declarativa desde los registros
  PROVIDER: `provider.codex.submit.repress_safe=true`,
  `confirm_timeout_seconds=6`, `event_source=cmux` (y las entradas kimi/
  opencode/claude completas).
- Send durable `677b1995-3326-4808-9a82-8753003aa969` → cadena de ledger
  `preparing → dispatched → authorized → running → succeeded`, con
  `submission_event_id=84697139-…-95` (exactamente un submit físico bound) y
  `reason=frontier_sentinel_verified`.
- Round-trip send→succeeded ≈ 30 s. Teardown limpio (`closed workspace:2`),
  árbol cmux de vuelta a un workspace idle.
- No-regresión del doble-submit: `authorize_prompt_submission` exige
  exactamente un `UserPromptSubmit`; un Enter duplicado habría dado
  `multiple … ambiguous`, no `succeeded`.

## Veredicto R1 — Kimi: **BLOCKED (DETÉN — slice nuevo)**

El binario instalado es **kimi-code 0.28.1** (`~/.kimi-code/bin/kimi`,
instalado 2026-07-20 20:18, después del audit v5): una reescritura completa
que reemplaza a kimi-cli. Perdidos: `info --json` (healthcheck del router),
`--thinking`, `--agent-file`, `--work-dir` y el protocolo Wire del hook
bridge. Nuevos: `acp` (Agent Client Protocol), `--plan`, `--skills-dir`,
`doctor`, `-p/--prompt` no-interactivo. No existe binario legacy (el `.bak`
es el mismo binario nuevo). El carril Kimi completo (launch contract, agente
read-only, bridge de eventos, ready_pattern, UNSAFE_FLAGS) requiere un slice
de migración con su propio recon — no se improvisó en medio del smoke.

## Veredicto F0 — opinion: **PASS** (segunda corrida)

- Corrida 1 (`20a44f2c`): builder OK a la primera (los flags `codex exec
  --model gpt-5.6-terra` funcionan tal cual, 28.0 s / 6262 chars); architect
  FAILED «Credit balance is too low» — `ANTHROPIC_API_KEY` filtrado en el
  entorno desvió `claude -p` del OAuth de suscripción a una cuenta API sin
  crédito. El arnés capturó status/exit/stderr fielmente y salió con código
  parcial.
- Corrida 2 (`3e7e7af9`, con `env -u ANTHROPIC_API_KEY`): **ambas
  perspectivas OK** — architect claude-sonnet-5 138.6 s / 9433 chars; builder
  gpt-5.6-terra 23.7 s / 5494 chars. Paralelismo real: wall total ≈ solo el
  architect. Artefactos en `outputs/fusion/3e7e7af9/opinion/` + línea
  canónica en `outputs/fusion/ledger.jsonl`.
- Calidad: el architect ancló su diseño leyendo los archivos reales del repo
  (propuesta congelada + `fusion_harness.py`); el builder entregó un borrador
  ejecutable del prompt de síntesis. Ambos artefactos son el input directo
  del diseño del mega-prompt F1.

## Carriles NO probados (honestos)

1. **Kimi submit en vivo** — bloqueado por la migración kimi-code (arriba).
   Vía de cierre: slice de migración, luego repetir este smoke.
2. **Recuperación del Enter tragado de Codex** — el camino re-press existe y
   está test-lockeado, pero el swallow no ocurrió naturalmente en esta
   corrida (confirmó al primer intento). Vía de cierre: se cierra solo por
   frecuencia de uso; no se puede forzar determinísticamente.
3. **Panel local de F0 (`--panel`)** — no ejercido en vivo (Ollama no era
   parte del criterio de done F0); cubierto por test con shim. Vía:
   `just opinion "<q>" --panel` con Ollama arriba.
4. **Fallback legacy de manifests pre-contrato** — cubierto por test; no hay
   fleet legacy vivo que ejercerlo. Vía: se retira cuando no queden manifests
   pre-R1.
5. **Filtrado de `ANTHROPIC_API_KEY` en el arnés** — la corrida 2 lo esquivó
   con `env -u`; decidir si el arnés debe sanear su entorno por defecto es
   una pregunta de la entrevista F1.
